#!/bin/bash
# start_monitoring_stack.sh (template)
# Start Loki and Grafana locally (templates). Use docker-compose or system packages in real lab.
echo "Starting monitoring stack (template). Review the commands before executing."
# Example: docker-compose -f monitoring/docker-compose.yml up -d
