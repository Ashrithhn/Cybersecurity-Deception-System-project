#!/bin/bash
# install_cowrie.sh (template)
# This is a template installation script — review and run step-by-step.
set -e
echo "Installing prerequisites (dummy step)"
echo "-> Update package lists"
# sudo apt update
echo "-> Create a cowrie user and virtualenv (example)"
# sudo useradd -m cowrie
# sudo apt install -y git python3-venv python3-dev libssl-dev libffi-dev build-essential
# sudo -u cowrie -H bash -c "git clone https://github.com/cowrie/cowrie.git /home/cowrie/cowrie && cd /home/cowrie/cowrie && python3 -m venv venv && ./venv/bin/pip install -r requirements.txt"
echo "Installation template complete. Customize before running."
